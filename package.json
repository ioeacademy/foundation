{
  "name": "foundation",
  "private": true,
  "version": "0.1.0",
  "description": "P2P educational content sharing - PoC",
  "workspaces": [
    "server",
    "pwa",
    "shared"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite --no-warnings server/index.js",
    "dev": "node --experimental-sqlite --no-warnings server/index.js"
  }
}
